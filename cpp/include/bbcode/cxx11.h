#ifndef BBCODE_CXX11_H
#define BBCODE_CXX11_H

namespace bbcode {

// Specifies that the type is POD (Plain Old Data) type. This means the type is compatible with the types used in the C programming language, that is, can be exchanged with C libraries directly, in its binary form.
// If T is a PODType ("plain old data type"), that is, both trivial and standard-layout, provides the member constant value equal to true. For any other type, value is false.

}

#endif // BBCODE_CXX11_H
